# Backend base URL — internal only, never exposed to the browser
# In Docker Compose use the service name: http://backend:10140
# For local dev point directly at the BE instance
API_BASE_URL=http://192.168.134.6:10140
